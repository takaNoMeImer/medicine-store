import { writable } from "svelte/store";

export const writeKeyComponent = writable("Defecto")
export const writeDataComponent = writable({})
export const numero = writable(10)
export const nombre = writable("Nombre")